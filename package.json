{
  "name": "text-based-browser-game-1",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bta": "pnpm --filter backend test",
    "btc": "pnpm --filter backend test --project concurrency",
    "bti": "pnpm --filter backend test --project integration",
    "btu": "pnpm --filter backend test --project unit",
    "checks": "pnpm lint:fix && pnpm format:fix && pnpm -r checks",
    "db:clean": "docker compose -f infra/docker-compose.yaml down --volumes && docker compose -f infra/docker-compose.yaml up --wait db && pnpm --filter backend db:migrate && pnpm --filter backend db:seed",
    "db:nuke": "rm -rf backend/drizzle && pnpm --filter backend db:generate --name init && pnpm db:clean",
    "format": "oxfmt --check",
    "format:fix": "oxfmt",
    "fte": "pnpm --filter frontend e2e",
    "lint": "oxlint --max-warnings 0",
    "lint:fix": "pnpm lint --fix",
    "map-gen": "node backend/scripts/map-gen/program.ts",
    "map-gen:all": "pnpm map-gen galaxy cluster --grid --seed 1234 && pnpm map-gen galaxy spiral --grid --seed 1234 && pnpm map-gen spiral --seed 1234 && pnpm map-gen cluster --seed 1234",
    "playtest": "node backend/scripts/solo-game/program.ts play",
    "prepare": "husky",
    "storybook": "pnpm --filter frontend storybook",
    "wts": "pnpm ci && cp ../text-based-browser-game-1/backend/.env backend/.env && cp ../text-based-browser-game-1/frontend/.env frontend/.env"
  },
  "devDependencies": {
    "@guillaume-docquier/oxfmt": "^1.0.6",
    "@guillaume-docquier/oxlint": "^1.4.2",
    "@guillaume-docquier/show-me": "^1.7.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.4.1",
    "oxfmt": ">=0.65.0",
    "oxlint": "^1.80.0",
    "oxlint-tsgolint": "^7.0.2001"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml,css,html}": "oxfmt --no-error-on-unmatched-pattern",
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt --no-error-on-unmatched-pattern"
    ]
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "26.8.1",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@11.24.0"
}
