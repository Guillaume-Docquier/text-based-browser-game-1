import type { Point2D } from "#lib/map/points/Point.ts"

/**
 * Generates a disc of the given radius containing nbPoints that follow a normal distribution around its origin
 */
export function discGenerator(_: { radius: number; nbPoints: number }): Point2D[] {
  return []
}
