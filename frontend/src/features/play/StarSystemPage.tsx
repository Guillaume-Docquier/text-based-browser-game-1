import { type ReactElement } from "react"

export function StarSystemPage(): ReactElement {
  // oxlint-disable-next-line no-warning-comments -- incoming
  return <section className="flex min-h-[34rem] min-w-0 flex-1 flex-col">{/* TODO */}</section>
}
